# 🖼️ Assignment 1: RGB & CMY Channel Splitting

## 📋 Problem Statement

**Input:** 24-bit color image (captured from WCE campus)  
**Expected Output:**
- Display the original image and its three **RGB** split channel images
- Display the original image and its three **CMY** split channel images

---

## 🧠 Concepts Involved

### 🎨 Color Models

- **RGB (Red, Green, Blue):** Standard color model for digital images.  
- **CMY (Cyan, Magenta, Yellow):** Used in color printing; each channel is the inverse of its RGB counterpart.

---

## 🛠️ Image Processing Steps

1. **Image Acquisition:** Capture or load a real-world image.
2. **Channel Splitting:** Separate the image into its color components.
3. **CMY Conversion:** Compute CMY channels from RGB.
4. **Visualization:** Display each channel for analysis.

---

## 📝 Code Explanation (Line by Line)

```python
import cv2
import numpy as np
import matplotlib.pyplot as plt
```
- **Why?**
  - `cv2`: For image reading and processing.
  - `numpy`: For numerical operations.
  - `matplotlib.pyplot`: For displaying images.

```python
img = cv2.imread('img.jpg')
img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)  # Convert BGR to RGB
```
- **Why?**
  - Reads the image file into a NumPy array (default in BGR format).
  - Converts image from OpenCV’s default BGR to standard RGB for correct color display in matplotlib.

```python
r, g, b = cv2.split(img)
```
- **Why?**
  - Splits the image into its Red, Green, and Blue channels for individual analysis.

```python
c = 255 - r
m = 255 - g
y = 255 - b
```
- **Why?**
  - Computes the CMY channels by inverting the RGB channels (CMY = 255 - RGB).

---

### 📊 Displaying RGB Channels

```python
plt.figure(figsize=(12, 8))
plt.subplot(2, 2, 1)
plt.title('Original Image')
plt.imshow(img)
plt.axis('off')

plt.subplot(2, 2, 2)
plt.title('Red Channel')
plt.imshow(r, cmap='Reds')
plt.axis('off')

plt.subplot(2, 2, 3)
plt.title('Green Channel')
plt.imshow(g, cmap='Greens')
plt.axis('off')

plt.subplot(2, 2, 4)
plt.title('Blue Channel')
plt.imshow(b, cmap='Blues')
plt.axis('off')
plt.tight_layout()
plt.show()
```
- **Why?**
  - Sets up a 2x2 grid for displaying images.
  - Shows the original image and each RGB channel with appropriate color maps.
  - `tight_layout()` ensures proper spacing, and `show()` renders the figure.

---

### 📊 Displaying CMY Channels

```python
plt.figure(figsize=(12, 8))
plt.subplot(2, 2, 1)
plt.title('Original Image')
plt.imshow(img)
plt.axis('off')

plt.subplot(2, 2, 2)
plt.title('Cyan Channel')
plt.imshow(c, cmap='winter')
plt.axis('off')

plt.subplot(2, 2, 3)
plt.title('Magenta Channel')
plt.imshow(m, cmap='magma')
plt.axis('off')

plt.subplot(2, 2, 4)
plt.title('Yellow Channel')
plt.imshow(y, cmap='YlOrBr')
plt.axis('off')
plt.tight_layout()
plt.show()
```
- **Why?**
  - Again, shows the original image for comparison.
  - Displays each CMY channel with visually distinct color maps.
  - `tight_layout()` ensures proper spacing, and `show()` renders the figure.

---

## ✅ Summary Table

| Step                | Purpose                                      | Function/Concept         |
|---------------------|----------------------------------------------|-------------------------|
| Import Libraries    | Image processing & visualization             | `cv2`, `numpy`, `matplotlib` |
| Read Image          | Load image into program                      | `cv2.imread`            |
| Convert Color Space | Ensure correct color display                 | `cv2.cvtColor`          |
| Split Channels      | Analyze individual color components          | `cv2.split`             |
| Compute CMY         | Get printing color model channels            | `255 - channel`         |
| Display Images      | Visualize results for understanding          | `matplotlib` subplots   |

---

## 🌟 Key Takeaways

- **Channel splitting** helps in understanding how colors combine in images.
- **CMY model** is crucial for printing and complements the RGB model.
- **Visualization** is essential for verifying image processing steps.

---

## 📝 Full Python Code

```python
import cv2
import numpy as np
import matplotlib.pyplot as plt

# Read the image
img = cv2.imread('img.jpg')
img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)  # Convert BGR to RGB

# Split the RGB channels
r, g, b = cv2.split(img)

# Create CMY channels
c = 255 - r
m = 255 - g
y = 255 - b

# Display RGB channels
plt.figure(figsize=(12, 8))
plt.subplot(2, 2, 1)
plt.title('Original Image')
plt.imshow(img)
plt.axis('off')

plt.subplot(2, 2, 2)
plt.title('Red Channel')
plt.imshow(r, cmap='Reds')
plt.axis('off')

plt.subplot(2, 2, 3)
plt.title('Green Channel')
plt.imshow(g, cmap='Greens')
plt.axis('off')

plt.subplot(2, 2, 4)
plt.title('Blue Channel')
plt.imshow(b, cmap='Blues')
plt.axis('off')
plt.tight_layout()
plt.show()

# Display CMY channels
plt.figure(figsize=(12, 8))
plt.subplot(2, 2, 1)
plt.title('Original Image')
plt.imshow(img)
plt.axis('off')

plt.subplot(2, 2, 2)
plt.title('Cyan Channel')
plt.imshow(c, cmap='winter')
plt.axis('off')

plt.subplot(2, 2, 3)
plt.title('Magenta Channel')
plt.imshow(m, cmap='magma')
plt.axis('off')

plt.subplot(2, 2, 4)
plt.title('Yellow Channel')
plt.imshow(y, cmap='YlOrBr')
plt.axis('off')
plt.tight_layout()
plt.show()
```